---
default_stages: [pre-commit]
fail_fast: true

repos:
  - repo: local
    hooks:
      - id: gapsense-docker-gate
        name: GapSense strict Docker validation gate
        entry: .githooks/pre-commit
        language: system
        pass_filenames: false
        always_run: true
