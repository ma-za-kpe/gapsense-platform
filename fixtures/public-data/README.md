# Public CI curriculum fixture

This synthetic, non-curriculum fixture proves that the platform fails closed unless both canonical
country roots exist. It contains no proprietary source text and makes no extraction-completeness
claim.

Hosted validation selects this directory through `GAPSENSE_DATA_HOST_PATH`. Local development
continues to mount the sibling `gapsense-data` repository by default.
